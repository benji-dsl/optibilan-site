---
import DesktopNavigation from './DesktopNavigation.astro';
import MobileNavigation from './MobileNavigation.astro';
---

<header class="fixed top-0 left-0 right-0 z-100 bg-blanc/95 backdrop-blur-sm border-b border-encre/10 transition-all duration-300" data-header>
  <nav class="container" aria-label="Navigation principale">
    <div class="flex h-16 items-center justify-between gap-4">
      <a href="/" class="flex items-center gap-2 shrink-0" aria-label="Optibilan - Accueil">
        <svg class="w-8 h-8 text-bleu-signature" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
          <circle cx="16" cy="16" r="14" stroke="#2A8AFF" stroke-width="2"/>
          <path d="M16 8v8M12 16h8" stroke="#2A8AFF" stroke-width="2" stroke-linecap="round"/>
        </svg>
        <span class="font-display font-bold text-xl text-encre">Optibilan</span>
      </a>

      <DesktopNavigation />
      <MobileNavigation />
    </div>
  </nav>
</header>

<script is:inline>
  const header = document.querySelector('[data-header]');
  let lastScroll = 0;
  window.addEventListener('scroll', () => {
    const currentScroll = window.pageYOffset;
    if (currentScroll > 100) {
      header?.classList.add('shadow-soft');
      header?.classList.add('bg-blanc/98');
    } else {
      header?.classList.remove('shadow-soft');
      header?.classList.remove('bg-blanc/98');
    }
    lastScroll = currentScroll;
  });
</script>

<style is:global>
  [data-header].shadow-soft {
    box-shadow: 0 2px 15px -3px rgba(0, 0, 0, 0.07), 0 10px 20px -2px rgba(0, 0, 0, 0.04);
  }
</style>